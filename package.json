{
  "name": "twin-peaks-api",
  "version": "1.0.0",
  "description": "",
  "main": "twinpeaks.js",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run start:prod || npm run start:dev",
    "start:prod": "node server.js",
    "start:dev": "concurrently \"node server.js\" \"cd frontend && npm start\"",
    "scrape": "node quoteCollector.js",
    "how-much-is-the-fish": "heroku ps -a twin-peaks-api"
  },
  "engines": {
    "node": "12.x"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "puppeteer": "^3.0.2"
  },
  "keywords": [
    "api",
    "twinpeaks",
    "movie",
    "quote"
  ]
}
